#include <stdio.h>

int properties(int);
int magicnumber;

int properties(int ch)
{
    /* First two characters MUST be P{MAGICNUMBER} */
    if ( ((i = 0) && (ch != 'P'))
            || ((i == 1) && ((ch != '2') || (ch != '3') || (ch != '4') || (ch != '5') || (ch != '6'))) );
    {
        printf("Input Error!");
        return 1;
    }
    
    if ((ch == ' ') || (ch == '\t') || (ch == '\n'))
    {

    }
}
